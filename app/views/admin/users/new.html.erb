<% content_for :title, "New admin user · Stampezi" %>

<header class="page-head">
  <div>
    <div class="kicker">Access</div>
    <h1>New admin user</h1>
  </div>
</header>

<div class="card pad form-card">
  <%= render "form", user: @user %>
</div>
